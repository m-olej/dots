LOCAL_PATH="$HOME/Documents/studia/notes"
REMOTE_PATH="gcp-backups:/Backups/College"

# Profile-specific exclusions
# Ensure the floating glob (**/) is used to catch these at any depth
PROFILE_EXCLUDES=(
    "**/.obsidian/**"
    "**/projects/**"
    "**/labs/**"
)
