# Setup system

```sh
git clone https://github.com/m-olej/dots.git ~/dotfiles
cd ~/dotfiles
chmod +x dotmanager.sh
chmod +x install.sh
./dotmanager.sh apply
./install.sh
```

## Change default shell
```sh
# chsh -s $(which zsh)
```
